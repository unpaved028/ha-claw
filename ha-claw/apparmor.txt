#include <tunables/global>

profile ha-claw flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  network,
  file,
  signal,
  capability chown,
  capability setuid,
  capability setgid,
  capability dac_override,
  capability dac_read_search,

  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/node rix,
  /entrypoint.sh rix,
  /app/** r,
  /data/** rw,
  /share/** rw,
  /tmp/** rwk,
  /dev/tty rw,
  /etc/passwd r,
  /etc/group r,
  /etc/nsswitch.conf r,
  /etc/ssl/** r,
  /etc/resolv.conf r,
}
