# Home Assistant add-on repository manifest.
#
# Users add this repository under:
#   Settings → Add-ons → Add-on Store → ⋮ → Repositories
#
# The Supervisor reads this file plus every config.yaml it finds below, so the add-on
# itself lives in ha-claw/. See https://developers.home-assistant.io/docs/add-ons/repository
name: HA-Claw Add-on Repository
url: https://github.com/unpaved028/ha-claw
maintainer: Rene Jung (https://github.com/unpaved028)
